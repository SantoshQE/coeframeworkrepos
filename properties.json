{
  "product": "CD-testrunner config template",
  "Version" : 0.1,
  "dev": {
    "maven": {
      "mvnoptions": "",
      "sh": "mvn clean test -Pdev -Dtest=TestRunner -s settings.xml",
      "bat": "mvn clean test -Pdev -Dtest=TestRunner -s settings.xml"
    }
  },
  "test": {
    "maven": {
      "mvnoptions": "",
      "sh": "mvn clean test -Pdev -Dtest=TestRunner -s settings.xml",
      "bat": "mvn clean test -Pdev -Dtest=TestRunner -s settings.xml"
    }
  }
}